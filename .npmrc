# Drizzle ORM declares optional peer deps on @op-engineering/op-sqlite and
# expo-sqlite (react-native side-stack), which we don't use. legacy-peer-deps
# lets npm proceed without erroring on those. Committed so Vercel honors it too.
legacy-peer-deps=true
